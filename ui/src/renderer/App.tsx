// src/App.tsx
import React from 'react';
import MediaBrowser from './MediaBrowser';

const App: React.FC = () => {
  return (
  <div>
      <h2>Media browser</h2>
      <MediaBrowser />
  </div>
  )
};

export default App;
