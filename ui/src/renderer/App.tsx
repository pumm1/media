// src/App.tsx
import React from 'react';
import MediaBrowser from './MediaBrowser';

const App: React.FC = () => {
  return (
  <div>
      Hello, Electron with React!
      <MediaBrowser />
  </div>
  )
};

export default App;
