import settingsImage from './gear.png'

const SettingsIcon = () => {
  return (
    <div>
      <img src={settingsImage} width={25} height={25} alt="Folder Icon" />
    </div>
  );
};

export default SettingsIcon